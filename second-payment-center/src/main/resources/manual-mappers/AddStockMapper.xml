<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.payment.center.manual.dao.AddStockMapper">

    <update id="addStock">
    update second_goods set goods_resp = goods_resp + #{quantity} where id=#{goodsId}
    </update>
</mapper>